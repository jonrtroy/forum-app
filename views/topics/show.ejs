<!DOCTYPE html>
<html>
  <head>
    <meta charset="utf-8">
    <title>Page Title</title>
  </head>
  <body>
    <main>
      <header>
        <h1><%= topic.title %></h1>
      </header>
      <section>
        <div class="topic-container">
          <h2>Content: <%= topic.content %></h2>
          <h2>User Name: <%= topic.username %></h2>
          <p>LIKES: <%= topic.likes %></p>
          <form action="/topic/<%= topic.id %>/like?_method=PUT&show=true" method="POST">
              <input type="submit" value="LIKE">
          </form>
          <form class="" action="<%= topic.id %>/edit" method="GET">
            <input type="submit" value="EDIT">
          </form>

          <br>
          <div class="comment-section">
          <% comments.forEach((comment) => { %>
            <p><%= comment.username %></p>
            <p><%= comment.comment %></p>
          <% }) %>
          </div>
            <div class="add-comment">
              <form class="" action="/topic/<%= topic.id %>" method="POST">
                <input type="text" name="comments[username]" placeholder="username" %>"><br>
                <textarea name="comments[comment]" rows="8" cols="80" placeholder="Add Comment here..."></textarea><br>
                <input type="submit" value="ADD COMMENT">
              </form>
            </div>
        </div>
      </section>
    </main>
  </body>
</html>
